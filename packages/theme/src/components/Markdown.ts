import { createElement as element, FC } from 'react'
import { css } from 'emotion'

export const Markdown: FC<{}> = ({}) => {
  return element('div', {
    className: css({
      // todo...
    }),
  })
}
